# recipedb-board — public board shell

This repo is the published recipe-board PWA (GitHub Pages): code only, fetching private
recipedb data at runtime. **Never commit recipe content here — no recipes, no `index.json`,
no thumbnails, no inbox notes, no screenshots of real recipes.** Its history is public and
not practically retractable. Orientation, push policy, and the pipeline live in the private
repo: `D:\Claude Projects\recipedb\CLAUDE.md`.
